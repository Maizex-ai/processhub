# Backups created by setup/restore next to live files (should never land here,
# but ignore just in case someone copies them in).
*.bak
*.bak-*

# Built extension packages (rebuilt by build-vsix.ps1)
*.vsix

# OS junk
.DS_Store
Thumbs.db
desktop.ini
